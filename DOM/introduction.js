// El objeto global en javascript es window.

// El window es el elemento donde se almacenan nuestras variables globales. 

window // -> Contiene toda la info del navegador y del documento

window.location(); // Nos devuelva la URL con otra información de la URL. 

window.innerWidth(); // Nos muestra el ancho del navegador. 
window.innerHeight(); // Nos muestra el alto del navegador.

// Podemos acceder a todo el documento con: 

document // es toda la parte visible de la página. 

// user agent -> nos brinda información del navegador y del dispositivo en el que se encuetran




